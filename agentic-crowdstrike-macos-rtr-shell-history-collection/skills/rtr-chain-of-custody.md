# rtr-chain-of-custody

**Phase:** collect

**Purpose:** Hash originals before copying, copy to `/tmp` with `shellhist_` prefix, read full contents with `cat`, zip the copies, hash the zip, retrieve it to Falcon cloud with `get`, and produce the forensic analysis.

## Skill Instructions

<!-- Paste your skill content below -->

```text

```
